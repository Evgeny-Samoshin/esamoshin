name: CI

on:
  push:
    branches: ["feat/hybrid-structure-router"]
  pull_request:
    branches: ["feat/hybrid-structure-router"]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo "Hello from SAMOSHIN CI"
